# Home

React and TypeScript portal built with Vite and SWC.

Run linting and type-checking from the workspace root with `pnpm lint`, or run
`pnpm lint` in this package to check only the home application. Formatting is
managed at the workspace root with Oxfmt.
